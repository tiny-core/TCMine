@* Emite as variáveis de token direto, sem HeadContent.

   HeadContent serve para páginas injetarem conteúdo no head a partir de
   dentro da árvore roteada. Aqui já estamos no head do App.razor, então
   o style vai direto. *@

<style>@((MarkupString)Css)</style>
