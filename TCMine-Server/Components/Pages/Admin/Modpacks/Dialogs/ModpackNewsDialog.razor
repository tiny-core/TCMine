@namespace TCMine_Server.Components.Pages.Admin.Modpacks.Dialogs

@* Modal de novidades do modpack (Fase 3): hospeda o NewsPanel, que é self-contained (carrega e grava
   por conta própria). Sem ações de salvar aqui — só fechar. Ver code-behind. *@

<MudDialog>
    <DialogContent>
        <NewsPanel ModpackId="ModpackId"/>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Close">Fechar</MudButton>
    </DialogActions>
</MudDialog>
